# Docker Compose Override Example
# Copy this file to docker-compose.override.yml and update values for local development
# docker-compose.override.yml is gitignored for security

version: '3.8'

services:
  postgres:
    environment:
      POSTGRES_PASSWORD: your-local-dev-password
      
  redis:
    command: redis-server --requirepass your-redis-password