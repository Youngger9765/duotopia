# Duotopia Environment Variables Example
# Copy this file to .env and update with your actual values
# NEVER commit .env file to version control

# GCP Configuration
GCP_PROJECT_ID=duotopia-469413

# Database Configuration (for docker-compose)
DB_PASSWORD=your-secure-database-password

# Frontend Configuration (if needed at root level)
VITE_API_URL=http://localhost:8000

# Note: Backend specific variables should go in backend/.env
# Note: Frontend specific variables should go in frontend/.env