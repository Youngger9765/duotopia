# Deploy to Production with Supabase
# 生產環境使用 Supabase (免費)
#
# 啟用方式：
# 1. mv deploy-production-supabase.yml.backup deploy-production.yml
# 2. git add -A && git commit -m "Enable production deployment" && git push

name: Deploy to Production (Supabase)

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

env:
  PROJECT_ID: duotopia-469413
  REGION: asia-east1
  BACKEND_SERVICE: duotopia-production-backend
  FRONTEND_SERVICE: duotopia-production-frontend
  REPOSITORY: duotopia-repo

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Deploy Production with Supabase
      run: |
        echo "🚀 Deploying to Production with Supabase (Free)"
        # Production deployment steps...