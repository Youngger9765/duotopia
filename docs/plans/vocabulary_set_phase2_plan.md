# 單字集 (VOCABULARY_SET) Phase 2 實作計劃

> **Issue**: #147
> **建立日期**: 2025-12-26
> **分支**: `claude/issue-147`
> **狀態**: 規劃中

---

## 1. 概述

### 1.1 功能摘要

| 項目 | 說明 |
|------|------|
| ContentType | `VOCABULARY_SET` |
| 中文名稱 | 單字集 |
| 練習模式 | 單字朗讀 / 單字選擇 |
| 目標使用者 | 教師（建立內容）、學生（練習） |

### 1.2 核心功能

**兩種練習模式**：

| 模式 | 中文 | 功能 | 計分 | 老師批改 |
|------|------|------|------|---------|
| `word_reading` | 單字朗讀 | 學生朗讀單字 | Azure Speech AI | ✅ 需要 |
| `word_selection` | 單字選擇 | 看單字/聽音檔選翻譯 | 艾賓浩斯熟悉度 | ❌ 不需要 |

---

## 2. 需求詳細說明

### 2.1 內容建立（教師端）

#### 建立方式
1. 在公版課程或班級課程列表中新增/編輯
2. 可複製公版課程

#### 批次貼上功能
- **格式**: 每一行是一個單字
- **生成選項**（預設全部勾選）:
  - ☑ AI 生成例句
  - ☑ AI 生成例句翻譯
  - ☑ 生成音檔 (TTS)
  - ☑ AI 生成單字翻譯
- **按鈕文案**: 「批次生成音檔」(EN: "Generate Audio")
- **AI 服務**: 使用與 AI 生成例句相同的服務

#### 圖片上傳功能
- **Windows**: 截圖後 Ctrl + V 貼上到 input 欄位
- **Mac**: 截圖後可拖曳進入 input
- 預設有上傳按鈕可點擊上傳
- **儲存位置**: GCS (Google Cloud Storage)

### 2.2 作業建立（教師端）

#### 流程
1. 選擇「單字集」內容類型
2. 選擇練習模式：「單字朗讀」或「單字選擇」
3. 設定模式專屬選項
4. 選擇學生
5. 填寫其他細節
6. 發布

#### 作業設定選項

| 選項 | 單字朗讀 | 單字選擇 | 預設值 | 說明 |
|------|:-------:|:-------:|--------|------|
| 達標熟悉度 | ❌ | ✅ | 80% | 老師可調整 |
| 打亂順序 | ✅ | ✅ | false | |
| 顯示翻譯 | ✅ | ❌ | true | |
| 顯示單字 | ❌ | ✅ | true | 單字選擇可隱藏單字，只播音檔 |
| 顯示圖片 | ✅ | ✅ | true | |
| 播放音檔 | 選填 | 選填 | false | |

### 2.3 作答畫面（學生端）

#### 單字朗讀流程
```
已指派 → 未開始 → 已開始 → 已提交 → 待訂正 → 已訂正 → 已完成
```

- 流程與「例句朗讀」類似
- **錄音限制**:
  - 最大時間: **5 秒**
  - 最小時間: **0.01 秒**（因不同裝置錄音檔案大小不同，不限制檔案大小）

#### 單字選擇流程
```
已指派 → 未開始 → 已開始 → 已完成
                          ↑
                   熟悉度達標自動完成
```

- **題型**:
  - 看英文單字，選中文翻譯
  - 聽音檔，選中文翻譯（僅當 play_audio=true 且 show_word=false）
- **干擾項**: AI 生成（**固定 3 個**，總共 4 選 1）
- **熟悉度進度**: 每完成一輪顯示目前熟悉度
- **達標提示**: 跳窗告知學生已達標，可選「關閉」或「繼續練習」

### 2.4 計分方式

#### 單字朗讀
- 使用 Azure Speech API 評分
- 維度: accuracy_score, fluency_score, pronunciation_score
- 需老師批改後完成

#### 單字選擇（艾賓浩斯記憶曲線）

**初始熟悉度**: 0%（新單字）

> **未來考量**: 考慮 by 學生角度記錄單字熟練度（跨班級作業），但牽涉帳號合併問題，暫時先以作業為單位記錄。

---

## 3. ⚠️ 向後兼容性考量

### 3.1 核心原則

**絕對不能影響現有的例句集功能！**

單字集是一個**新增功能**，而非修改功能。所有新增的代碼、資料庫欄位、API 都必須確保：
1. 不破壞現有例句集作業的創建
2. 不破壞現有例句集作業的顯示
3. 不破壞現有例句集作業的答題流程
4. 不破壞現有學生作業數據

### 3.2 關鍵檢查點

#### 資料庫層面

```sql
-- ✅ 正確做法：添加欄位時必須設置預設值
ALTER TABLE assignments ADD COLUMN IF NOT EXISTS
  target_proficiency INTEGER DEFAULT 80;

-- ❌ 錯誤做法：不設置預設值會導致現有記錄出現 NULL
ALTER TABLE assignments ADD COLUMN target_proficiency INTEGER NOT NULL;
```

**檢查清單**：
- [ ] 所有新欄位有預設值，確保現有資料不受影響
- [ ] 新增的表不影響現有表
- [ ] 所有 Foreign Key 使用 `ON DELETE CASCADE` 避免孤兒記錄

#### API 層面

```python
# ✅ 正確做法：新參數為選填，有預設值
class AssignmentCreate(BaseModel):
    practice_mode: str = "reading"  # ✅ 有預設值
    target_proficiency: int = 80    # ✅ 有預設值

# ❌ 錯誤做法：必填欄位會導致舊版前端無法創建作業
class AssignmentCreate(BaseModel):
    practice_mode: str  # ❌ 必填會破壞向後兼容
```

#### 前端路由層面

```typescript
// ✅ 正確做法：根據 content.type 路由
const renderActivity = () => {
  const contentType = assignment.content?.type;

  if (contentType === "EXAMPLE_SENTENCES") {
    return <ExampleSentencesActivity {...props} />;
  }

  if (contentType === "VOCABULARY_SET") {
    return <VocabularyActivity {...props} />;  // 新的單字集組件
  }

  return <DefaultTemplate />;
};
```

### 3.3 紅線規則（絕對禁止）

1. ❌ **絕對不要**修改現有的 `EXAMPLE_SENTENCES` 相關代碼邏輯
2. ❌ **絕對不要**在例句集流程中調用單字選擇的記憶曲線函數
3. ❌ **絕對不要**讓新參數成為必填欄位
4. ❌ **絕對不要**在沒有備份的情況下執行 Migration
5. ❌ **絕對不要**刪除或重命名現有的資料庫欄位
6. ❌ **絕對不要**修改現有 API 的回傳格式（只能新增欄位）

---

## 4. 已存在的資料庫架構

### 4.1 已存在的資料表 ✅

以下資料表和欄位已經透過 migration `666c291b851d` 建立：

#### user_word_progress（記憶強度追蹤）

```sql
CREATE TABLE user_word_progress (
    id SERIAL PRIMARY KEY,
    student_id INTEGER NOT NULL,
    student_assignment_id INTEGER NOT NULL,
    content_item_id INTEGER NOT NULL,

    -- 艾賓浩斯記憶曲線欄位
    memory_strength NUMERIC(5, 4) DEFAULT 0,      -- 記憶強度 (0-1)
    repetition_count INTEGER DEFAULT 0,            -- 連續答對次數
    correct_count INTEGER DEFAULT 0,               -- 累計答對
    incorrect_count INTEGER DEFAULT 0,             -- 累計答錯
    last_review_at TIMESTAMPTZ,                    -- 最後複習時間
    next_review_at TIMESTAMPTZ,                    -- 下次複習時間
    easiness_factor NUMERIC(3, 2) DEFAULT 2.5,    -- 難易度因子 (SM-2)
    interval_days NUMERIC(10, 2) DEFAULT 1,        -- 複習間隔天數
    total_attempts INTEGER DEFAULT 0,              -- 總嘗試次數
    accuracy_rate NUMERIC(5, 4) DEFAULT 0,         -- 正確率

    UNIQUE(student_assignment_id, content_item_id)
);
```

#### practice_sessions（練習 Session）

```sql
CREATE TABLE practice_sessions (
    id SERIAL PRIMARY KEY,
    student_id INTEGER NOT NULL,
    student_assignment_id INTEGER NOT NULL,
    practice_mode VARCHAR(20) NOT NULL,  -- 'listening' / 'writing'
    words_practiced INTEGER DEFAULT 0,
    correct_count INTEGER DEFAULT 0,
    total_time_seconds INTEGER DEFAULT 0,
    started_at TIMESTAMPTZ DEFAULT NOW(),
    completed_at TIMESTAMPTZ
);
```

#### practice_answers（答題記錄）

```sql
CREATE TABLE practice_answers (
    id SERIAL PRIMARY KEY,
    practice_session_id INTEGER NOT NULL,
    content_item_id INTEGER NOT NULL,
    is_correct BOOLEAN NOT NULL,
    time_spent_seconds INTEGER DEFAULT 0,
    answer_data JSONB  -- 詳細答題資料
);
```

### 4.2 已存在的 PostgreSQL 函數 ✅

#### update_memory_strength()

SM-2 演算法實作，更新記憶強度：
- 答對: 增加 memory_strength，根據連續次數調整 interval
- 答錯: 降低 memory_strength，重置 interval 為 1 天

#### get_words_for_practice()

智能選題演算法：
- 優先選擇從未練習過的單字 (priority = 100)
- 其次選擇已到複習時間且記憶強度低的單字 (priority = 50-100)
- 最後選擇記憶強度低但未到複習時間的單字 (priority = 0-30)

#### calculate_assignment_mastery()

計算作業整體熟悉度：
- 回傳: current_mastery, target_mastery, achieved, words_mastered, total_words
- 預設目標: 90%（需調整為可配置的 target_proficiency）

### 4.3 需要新增的資料庫欄位

#### Assignment 表擴充

```sql
-- 單字集專用設定欄位
ALTER TABLE assignments ADD COLUMN IF NOT EXISTS
    target_proficiency INTEGER DEFAULT 80;

ALTER TABLE assignments ADD COLUMN IF NOT EXISTS
    show_translation BOOLEAN DEFAULT true;

ALTER TABLE assignments ADD COLUMN IF NOT EXISTS
    show_word BOOLEAN DEFAULT true;

ALTER TABLE assignments ADD COLUMN IF NOT EXISTS
    show_image BOOLEAN DEFAULT true;
```

#### ContentItem 表擴充（確認現有欄位）

```sql
-- 確認以下欄位存在，若不存在則新增
ALTER TABLE content_items ADD COLUMN IF NOT EXISTS
    image_url TEXT;

ALTER TABLE content_items ADD COLUMN IF NOT EXISTS
    part_of_speech VARCHAR(20);
```

---

## 5. 艾賓浩斯記憶曲線演算法

### 5.1 核心公式

基於 [Ebbinghaus Forgetting Curve](https://en.wikipedia.org/wiki/Forgetting_curve) 和 SM-2 演算法：

```
記憶保持率 R = e^(-t/S)
```

- **R**: 記憶保持率 (0-1)
- **t**: 距上次複習的時間（秒）
- **S**: 記憶強度常數（與 easiness_factor 成正比）

### 5.2 熟悉度更新邏輯（已實作於 update_memory_strength）

| 動作 | 效果 |
|------|------|
| **答對** | memory_strength += 0.3（最高 1.0）<br>easiness_factor 增加<br>interval_days 根據 SM-2 增長 |
| **答錯** | memory_strength *= 0.5（最低 0.1）<br>easiness_factor -= 0.2<br>repetition_count 重置為 0<br>interval_days 重置為 1 |

### 5.3 間隔重複規則（SM-2）

| 連續答對次數 | 下次複習間隔 |
|-------------|-------------|
| 第 1 次 | 1 天 |
| 第 2 次 | 6 天 |
| 第 3+ 次 | interval_days * easiness_factor |

### 5.4 達標判定

- **達標條件**: 所有單字的平均熟悉度 >= target_proficiency（預設 80%）
- **完成流程**:
  1. 每輪練習結束後檢查熟悉度
  2. 達標時跳出提示視窗
  3. 學生可選擇「關閉」或「繼續練習」
  4. 選擇關閉後，作業狀態更新為「已完成」

### 5.5 干擾項生成

- **數量**: 固定 **3 個**干擾項 + 1 個正確答案 = **4 選 1**
- **AI 服務**: 使用與「AI 生成例句」相同的服務
- **生成策略**:
  - 語義相近（同類別詞彙）
  - 拼寫相似（易混淆詞）
  - 同詞性（名詞配名詞）

```python
async def generate_distractors(word: str, translation: str) -> List[str]:
    """
    使用 AI 生成 3 個干擾項

    Args:
        word: 英文單字 (e.g., "apple")
        translation: 正確翻譯 (e.g., "蘋果")

    Returns:
        3 個干擾項翻譯 (e.g., ["香蕉", "橘子", "葡萄"])
    """
    prompt = f"""
    為單字 "{word}"（正確翻譯: {translation}）生成 3 個干擾項。
    要求:
    - 必須是中文翻譯
    - 語義相近但不同義（例如都是水果）
    - 同詞性
    - 不能是同義詞
    - 不能是正確答案

    只回傳 3 個干擾項，以逗號分隔。
    """
    # 使用與 AI 生成例句相同的服務
    result = await ai_service.generate(prompt)
    return result.split(",")[:3]
```

---

## 6. 實作階段規劃

### Phase 2-1: 資料庫擴充與內容建立功能

**目標**: 擴充資料庫欄位，教師可以建立和管理單字集

#### 任務清單

1. **資料庫 Migration**
   - [ ] Assignment 新增 target_proficiency, show_translation, show_word, show_image
   - [ ] ContentItem 確認/新增 image_url, part_of_speech
   - [ ] 修改 calculate_assignment_mastery 使用可配置的 target_proficiency

2. **後端 API**
   - [ ] 批次建立單字 API
   - [ ] 圖片上傳 API (GCS)
   - [ ] AI 生成例句 API（沿用現有服務）
   - [ ] 批次生成音檔 API

3. **前端組件**
   - [ ] VocabularyContentForm 組件
   - [ ] BatchWordImport 組件（每行一個單字）
   - [ ] ImageUploader 組件（支援貼上/拖曳/點擊）

### Phase 2-2: 單字朗讀功能

**目標**: 學生可以進行單字朗讀練習

#### 任務清單

1. **前端組件**
   - [ ] WordReadingTemplate 組件
   - [ ] 錄音組件（max 5s, min 0.01s，**不限檔案大小**）
   - [ ] 單字卡片顯示（圖片、翻譯、音檔）

2. **後端 API**
   - [ ] 取得單字朗讀題目 API
   - [ ] 上傳錄音 API（沿用現有）
   - [ ] Azure Speech 評分整合（沿用現有）

3. **老師端**
   - [ ] 單字朗讀批改介面
   - [ ] 評分和回饋功能

### Phase 2-3: 單字選擇功能

**目標**: 學生可以進行單字選擇練習

#### 任務清單

1. **前端組件**
   - [ ] WordSelectionActivity 組件
   - [ ] 選擇題 UI（4 選 1）
   - [ ] 熟悉度進度顯示
   - [ ] 達標提示視窗（關閉/繼續練習）

2. **後端 API**
   - [ ] 取得單字選擇題目 API（使用現有 get_words_for_practice）
   - [ ] AI 干擾項生成 API（使用現有 AI 服務）
   - [ ] 提交選擇答案 API（使用現有 update_memory_strength）
   - [ ] 取得熟悉度狀態 API（使用現有 calculate_assignment_mastery）

3. **作業完成邏輯**
   - [ ] 達標後自動更新作業狀態為「已完成」
   - [ ] 達標提示 UI

---

## 7. UI/UX 設計

### 7.1 單字朗讀畫面

```
┌─────────────────────────────────────────────┐
│ 單字朗讀                        第 3/10 題   │
├─────────────────────────────────────────────┤
│                                             │
│   ┌─────────────────────────────────┐       │
│   │         [圖片區域]              │       │
│   │           🍎                    │       │
│   └─────────────────────────────────┘       │
│                                             │
│         apple                               │
│         蘋果                                │
│                                             │
│         [🔊 播放音檔]                        │
│                                             │
├─────────────────────────────────────────────┤
│         [🎤 開始錄音]                        │
│                                             │
│   錄音時間: 00:00 / 00:05                   │
│   ⚠️ 錄音最少 0.01 秒，最多 5 秒             │
└─────────────────────────────────────────────┘
```

### 7.2 單字選擇畫面

```
┌─────────────────────────────────────────────┐
│ 單字選擇                   熟悉度: 65%      │
│ ████████████░░░░░░░░                        │
├─────────────────────────────────────────────┤
│                                             │
│   ┌─────────────────────────────────┐       │
│   │         [圖片區域]              │       │
│   │           🍎                    │       │
│   └─────────────────────────────────┘       │
│                                             │
│         apple                               │
│         [🔊 播放音檔]                        │
│                                             │
│   請選擇正確的翻譯：                         │
│                                             │
│   ┌───────────┐  ┌───────────┐             │
│   │   蘋果    │  │   香蕉    │             │
│   └───────────┘  └───────────┘             │
│   ┌───────────┐  ┌───────────┐             │
│   │   橘子    │  │   葡萄    │             │
│   └───────────┘  └───────────┘             │
│                                             │
└─────────────────────────────────────────────┘
```

### 7.3 達標提示視窗

```
┌─────────────────────────────────────────────┐
│             🎉 恭喜達標！                    │
├─────────────────────────────────────────────┤
│                                             │
│   您已達成目標熟悉度 80%                     │
│                                             │
│   目前熟悉度: 82%                            │
│   已熟練單字: 24 / 30 個                     │
│   總練習時間: 15 分鐘                        │
│                                             │
│   ┌───────────┐  ┌───────────┐             │
│   │   關閉    │  │ 繼續練習  │             │
│   └───────────┘  └───────────┘             │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 8. 測試計劃

### 8.1 單元測試

```python
# tests/test_vocabulary_proficiency.py

def test_initial_proficiency_is_zero():
    """新單字的初始熟悉度應為 0"""
    # 新建一個 user_word_progress
    # 驗證 memory_strength = 0

def test_proficiency_increase_on_correct():
    """答對應增加熟悉度"""
    result = update_memory_strength(sa_id, item_id, is_correct=True)
    assert result.memory_strength > 0

def test_proficiency_decrease_on_incorrect():
    """答錯應降低熟悉度"""
    # 先答對幾次建立熟悉度
    update_memory_strength(sa_id, item_id, True)
    update_memory_strength(sa_id, item_id, True)
    # 然後答錯
    result = update_memory_strength(sa_id, item_id, False)
    assert result.repetition_count == 0

def test_distractor_generation():
    """干擾項生成應回傳 3 個不重複的選項"""
    distractors = generate_distractors("apple", "蘋果")
    assert len(distractors) == 3
    assert "蘋果" not in distractors
```

### 8.2 整合測試

```python
# tests/integration/test_vocabulary_flow.py

def test_word_reading_complete_flow():
    """測試單字朗讀完整流程"""
    # 1. 建立單字集
    # 2. 建立作業 (practice_mode='word_reading')
    # 3. 學生開始練習
    # 4. 上傳錄音 (0.01s - 5s)
    # 5. AI 評分
    # 6. 老師批改
    # 7. 作業完成

def test_word_selection_complete_flow():
    """測試單字選擇完整流程"""
    # 1. 建立單字集
    # 2. 建立作業 (practice_mode='word_selection', target_proficiency=80)
    # 3. 學生開始練習
    # 4. 生成干擾項 (3 個)
    # 5. 提交答案
    # 6. 檢查熟悉度更新
    # 7. 多輪練習直到達標
    # 8. 作業自動完成

def test_backward_compatibility():
    """測試向後兼容性"""
    # 1. 建立例句集作業（不含新欄位）
    # 2. 驗證例句集作業正常運作
    # 3. 驗證新欄位有正確的預設值
```

### 8.3 向後兼容性測試場景

#### Scenario 1: 現有例句集作業不受影響
1. 使用新版代碼啟動系統
2. 查詢現有例句集作業
3. 確認新欄位自動填充預設值
4. 學生完成例句集作業
5. 確認評分和記錄正常

#### Scenario 2: 創建新的單字集作業
1. 老師選擇 `VOCABULARY_SET` 類型
2. 選擇「單字朗讀」或「單字選擇」模式
3. 設定相關選項
4. 學生完成作業
5. 確認流程正確

---

## 9. API 端點總覽

### 內容管理 API（新增）

| 端點 | 方法 | 用途 |
|------|------|------|
| `/api/teachers/contents/vocabulary-set/batch-create` | POST | 批次建立單字 |
| `/api/teachers/contents/vocabulary-set/generate-audio` | POST | 批次生成音檔 |
| `/api/teachers/contents/vocabulary-set/generate-sentences` | POST | AI 生成例句 |
| `/api/teachers/contents/upload-image` | POST | 上傳圖片 (GCS) |

### 學生練習 API

| 端點 | 方法 | 用途 | 狀態 |
|------|------|------|------|
| `/api/students/assignments/{id}/vocabulary/activities` | GET | 取得單字練習題目 | 待開發 |
| `/api/students/assignments/{id}/vocabulary/submit-reading` | POST | 提交單字朗讀 | 待開發 |
| `/api/students/assignments/{id}/vocabulary/submit-selection` | POST | 提交單字選擇 | 待開發 |
| `/api/students/assignments/{id}/vocabulary/proficiency` | GET | 取得熟悉度 | 待開發 |
| `/api/vocabulary/generate-distractors` | POST | AI 生成干擾項 | 待開發 |

### 可複用的現有 API

| 端點 | 用途 |
|------|------|
| `/api/students/upload-recording` | 上傳錄音檔 |
| `/api/speech/assess` | Azure Speech AI 評分 |
| `/api/tts/generate` | TTS 生成音檔 |

---

## 10. 前端組件結構

```
frontend/src/components/
├── activities/
│   ├── VocabularyActivity.tsx          # 主組件（根據 practice_mode 路由）
│   ├── WordReadingTemplate.tsx         # 單字朗讀
│   └── WordSelectionActivity.tsx       # 單字選擇
├── teacher/
│   ├── VocabularyContentForm.tsx       # 單字集編輯表單
│   ├── BatchWordImport.tsx             # 批次匯入
│   └── ImageUploader.tsx               # 圖片上傳組件
└── shared/
    ├── ProficiencyProgress.tsx         # 熟悉度進度條
    ├── WordCard.tsx                    # 單字卡片
    └── SelectionOptions.tsx            # 4 選 1 選項組件
```

---

## 11. 確認事項摘要

| 項目 | 確認結果 |
|------|---------|
| 錄音限制 | 時間限制（min 0.01s, max 5s），不限檔案大小 |
| 干擾項數量 | 固定 3 個（4 選 1） |
| 干擾項生成服務 | 使用與 AI 生成例句相同的服務 |
| 圖片儲存 | GCS |
| 初始熟悉度 | 0% |
| 跨班級熟悉度 | 暫以作業為單位，未來考量學生角度（帳號合併後） |

---

## 12. 參考資料

- [Ebbinghaus Forgetting Curve - Wikipedia](https://en.wikipedia.org/wiki/Forgetting_curve)
- [SM-2 Algorithm](https://www.supermemo.com/en/blog/application-of-a-computer-to-improve-the-results-obtained-in-working-with-the-supermemo-method)
- [CONTENT_TYPES.md](../CONTENT_TYPES.md)
- [Migration 666c291b851d](../../backend/alembic/versions/20251112_1045_666c291b851d_add_sentence_making_phase1_tables_and_.py)

---

**文件版本**: v2.0
**建立日期**: 2025-12-26
**最後更新**: 2025-12-26
**作者**: Claude Code
