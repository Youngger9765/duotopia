FROM python:3.11-slim
WORKDIR /app
COPY service-account-key.json ./
RUN cat service-account-key.json | python3 -m json.tool > /dev/null && \
    echo "✅ JSON file is valid" || echo "❌ JSON file is invalid"
CMD ["echo", "Test complete"]
